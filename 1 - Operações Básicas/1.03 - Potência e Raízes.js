let valor1 = 4;
let valor2 = 3;
const potencia = Math.pow(valor1, valor2); //Primeira entrada é base e a segunda é expoente.
const potencia2 = valor1 ** valor2; //Mesma ideia da função Math.pow().
const raizQuadrada = Math.sqrt(4); //Ou const raizQuadrada = valor * valor.
const raizOrdemSuperior = Math.pow(valor1, 1/valor2);

console.log("Potência:",potencia);
console.log("Raíz Quadrada:",raizQuadrada);
console.log("Raíz de Ordem Superior:",raizOrdemSuperior);
