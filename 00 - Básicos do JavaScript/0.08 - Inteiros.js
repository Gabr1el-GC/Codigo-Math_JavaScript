const decimalInteiroLiteral = 123456789;

const octal = 0o35; //Está em octal, que só pode incluir os números de 0 a 7.

const hexadecimal = 0xA2d; //Inclui as letras a-f e A-F.

const binario = 0b101001 //Apenas 0 e 1.

console.log(
    decimalInteiroLiteral,
    octal, 
    hexadecimal,
    binario
    )
    